name: 📺 Submit a tutorial
description: Share an OpenDesign tutorial video to be featured on open-design.ai/tutorials.
title: "[Tutorial]: "
labels: ["tutorials"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for sharing! Made or found a great OpenDesign tutorial on YouTube? Fill in the video link below — that's the only required field. A maintainer will review it and add it to the tutorials page. No pull request or markdown needed.

  - type: input
    id: video_url
    attributes:
      label: YouTube video URL
      description: Paste the full link, e.g. https://www.youtube.com/watch?v=XXXXXXXXXXX
      placeholder: https://www.youtube.com/watch?v=...
    validations:
      required: true

  - type: input
    id: author
    attributes:
      label: Creator / channel name
      description: Who made the video? Leave blank to use the YouTube channel name.
      placeholder: e.g. Your channel name
    validations:
      required: false

  - type: dropdown
    id: category
    attributes:
      label: Category
      description: Pick the closest fit (optional — a maintainer will confirm).
      options:
        - Getting started
        - Tutorial
        - Demo
        - Review
        - Community
      default: 1
    validations:
      required: false

  - type: textarea
    id: summary
    attributes:
      label: What does it cover?
      description: One or two sentences on what the video shows (optional).
      placeholder: A quick walkthrough of installing OpenDesign and shipping a first design.
    validations:
      required: false

  - type: checkboxes
    id: confirm
    attributes:
      label: Before you submit
      options:
        - label: This video is actually about OpenDesign (nexu-io/open-design), not a different product.
          required: true
